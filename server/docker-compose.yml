version: '3.8'
services:
  db:
    image: postgres:15
    container_name: skybox_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pakaruL123!
      POSTGRES_DB: skybox_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
